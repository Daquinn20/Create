{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python estimates_tracker.py --universe both",
    "restartPolicyType": "NEVER"
  }
}
